= simple_form_for @photo, html: {multipart:true} do |form|
  - if @photo.errors.any?
    #errors
      %h2
        = pluralize(@photo.errors.count, "erreur")
        ! L'image n'a pas pu être téléchargée.
        %ul
        - @photo.errors.full_message.each do |msg|
          %li= msg

    .form-group
      = form.input :title, input_html: { class: "form-control"}

    .form-group
      = form.input :description, input_html: { class: "form-control"}

    = form.button :submit, class: "btn btn-info"